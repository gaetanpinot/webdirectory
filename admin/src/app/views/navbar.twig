{% block navbar %}
<div id="navbar">
    <p id="titreNavbar">Webdir Admin</p>
    <a href="{{url_for('racine')}}">Accueil</a>
    <a href="{{url_for('createPersonne')}}">Créer personne</a>
    <a href="{{ url_for('createService') }}">Créer service</a>
    {% if isSuperAdmin==true %}
    <a href="{{url_for('createAdmin')}}">Créer admin</a>
    {% endif %}
</div>
{% endblock %}
